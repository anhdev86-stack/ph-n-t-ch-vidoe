# API
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
PyJWT==2.10.1
httpx==0.28.1

# LLM (0.120+ mới hỗ trợ output_config / structured output)
anthropic==0.120.2

# ASR (chạy CPU trên VPS)
faster-whisper==1.1.1
requests==2.32.3        # faster-whisper utils.py cần (tải model/VAD)

# Đọc tài liệu skill upload (.pdf) cho tính năng Huấn luyện AI
pypdf==5.1.0

# Tải video TikTok + impersonation. curl_cffi 0.12 khớp yt-dlp 2026.7 (0.16 quá mới -> targets unavailable).
# LƯU Ý: video giỏ hàng/affiliate TikTok chỉ lộ luồng AUDIO -> xem bằng player nhúng TikTok ở frontend.
yt-dlp[default]>=2026.7.4
curl_cffi==0.12.0
