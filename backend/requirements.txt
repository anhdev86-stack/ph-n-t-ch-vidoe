# API
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
PyJWT==2.10.1
httpx==0.28.1

# LLM (0.120+ mới hỗ trợ output_config / structured output)
anthropic==0.120.2

# ASR (chạy CPU trên VPS)
faster-whisper==1.1.1
requests==2.32.3        # faster-whisper utils.py cần (tải model/VAD)

# Tải video TikTok. [default] kéo theo curl_cffi ĐÚNG phiên bản cho "impersonation"
# -> TikTok mới trả luồng VIDEO (thiếu/lệch version -> chỉ có audio, đen hình).
yt-dlp[default]>=2026.7.4
